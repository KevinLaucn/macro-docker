# Macro 功能开关、架构级控制与权限体系说明

本文档基于 `KevinLaucn/macro` 源码与腾讯云 SG 生产环境（`marc-sg-2h8g`，域名 `chat.chnprints.com`）的真实运行时状态进行分类整理与去重。

---

## 一、核心维度划分：架构级 vs 功能级 vs 权限级

Macro 的配置开关分为三个核心层次：

1. **🏛️ 架构级开关（Architecture & Infrastructure）**：
   - **影响范围**：决定网络协议、认证链路、数据通道、后端微服务编排、异步消息队列以及客户端底层缓存。
   - **典型特征**：开关变化会改变服务间通信拓扑或依赖特定后端容器（如 `document_cognition_service`、`scheduled_action_service`、`image_proxy_service` 等）。如果后端未部署对应服务而强行开启，会导致网络请求报 404/502、认证死锁或客户端无限重试。
2. **✨ 功能级开关（Product Features & UI Capabilities）**：
   - **影响范围**：控制界面入口、操作面板、富文本扩展、交互组件和业务视图（如日历、划词 AI 胶囊、文档行级评论、反向链接等）。
   - **典型特征**：依赖既有核心业务服务（如 MacroDB、DSS、Email Service），开闭仅控制前端功能可见性与组件挂载，不会破坏底层网络与鉴权架构。
3. **🛡️ 权限与角色（RBAC & Permissions）**：
   - **影响范围**：控制用户等级、企业版去水印、管理面板访问权限、付费 AI 模型调用配额。
   - **核心机制**：由后端鉴权接口（`/auth/me`）读取 MacroDB 的 `roles_and_permissions` 表，响应 `user.permissions` 给前端 `useHasPermission()`。

---

## 二、配置与代码中存在的 4 大类冗余排查

经过对 `apps/web/src` 源码与生产编排的深度检索，当前系统中存在以下 4 大类重复与冗余：

### 1. ⚠️ 双轨别名重复（Twin Switch Aliasing）
在 `self-host/docker-compose.yml` 中，同时向前端注入了顶层 `ENABLE_XXX` 和嵌套的 `FEATURES.xxx`。
代码中 `featureFlags.ts` 的 `resolveCapability` 会按优先级读取两者（优先顶层 `ENABLE_XXX`，回退到 `FEATURES.xxx`）。
- `ENABLE_COGNITION` 与 `FEATURES.cognition`（完全等价，指向文档认知服务）
- `ENABLE_SCHEDULED_ACTIONS` 与 `FEATURES.scheduledActions`（完全等价，指向定时动作服务）
- `ENABLE_AGENTS` 与 `FEATURES.agents`（完全等价，指向智能体调度）
- `ENABLE_DOCS_COLLAB` 与 `FEATURES.docsCollab`（完全等价，指向画布协同 Token 生成）
- `ENABLE_NEW_APP_VIEWS` 与 `FEATURES.enableNewAppViews`（完全等价，指向新版组合视图）
- `ENABLE_CRM` 与 `FEATURES.enableCrm`（完全等价，指向客户关系模块）
> **去重结论**：这是历史遗留的双轨别名。两处定义不仅冗余，还容易引发冲突（例如曾出现外层配了 true，内层写了 false）。自托管只需统一维护外层 `ENABLE_XXX` 即可。

### 2. ⚠️ 未被前端消费的“虚设”环境变量（Unused Environment Variables）
在 `docker-compose.yml` 中注入了 `window.__MACRO_ENV__.PERMISSIONS` 包含 9 个 `PERM_*` 开关（`PERM_ALL_ACCESS`, `PERM_SUPER_ADMIN`, `PERM_CORPORATE`, `PERM_PRO_LICENSE`, `PERM_SUB_OPUS`, `PERM_AI_SUBSCRIBER`, `PERM_EDITOR_USER`, `PERM_ORGANIZATION_IT`, `PERM_EMAIL_TOOL`）。
> **源码实测证据**：全量检索前端 `apps/web/src`，**没有任何组件或逻辑去读取 `window.__MACRO_ENV__.PERMISSIONS`**。
> 前端 `useHasPermission()` 唯一依赖的是当前登录用户上下文 `userInfo()?.permissions`，它完全来自后端 `/auth/me` 接口查询数据库 `roles_and_permissions` 表的结果。
> **去重结论**：这 9 个 `PERM_*` 环境变量属于展示性/冗余注入，不具备真实授权能力，真正的特权需通过数据库角色赋予。

### 3. ⚠️ 主从层级依赖冗余（Hierarchical Overlap）
- **智能体体系**：`ENABLE_AGENTS` 是底层总能力开关；`ENABLE_CHAT_V3_AGENTS`（频道内@）和 `ENABLE_CURSOR_AGENTS`（Cursor 集成）是子入口。源码中子开关默认直接等于 `ENABLE_AGENTS`，在总开关未开启时子开关无意义。
- **邮件体系**：`ENABLE_EMAIL` 是邮件核心微服务总开关；签名、定时发送、多邮箱等均为其子功能。

### 4. ⚠️ 命名规范重复（PostHog Key 与 Env Key 重复列出）
官方上游最初将部分灰度开关命名为中划线（如 `enable-home-view`、`enable-soup-group-by`、`enable-task-duplicates`），通过 PostHog 下发；本 fork 扩展了对应的环境变量（`ENABLE_HOME_VIEW`、`ENABLE_SOUP_GROUP_BY`、`ENABLE_TASK_DUPLICATES`）。
旧文档把同一种功能拆成两张大表分别记录，造成了双重罗列。
> **去重结论**：合并为统一表格，直接在同一行展示环境变量与对应 PostHog 内部标识。

---

## 三、生产环境（腾讯云 SG）实际生效状态（已去重纯净版）

数据来源自生产环境实测：
- 生产配置：`/home/ubuntu/marco/.env` 与 `docker-compose.yml`
- 生产实际提供的前端配置：`https://chat.chnprints.com/env-config.js`
- 生产实际运行容器集群：25 个微服务与基础设施全栈容器

### 1. 🏛️ 架构与基础设施级开关（共 11 项，已合并别名）

| 统一配置项 | 历史别名 / 内部键名 | 生产状态 | 架构属性与生产考量 |
| :--- | :--- | :---: | :--- |
| `ENABLE_BEARER_TOKEN_AUTH` | `VITE_ENABLE_BEARER_TOKEN_AUTH` | ❌ **关 (false)** | **关键架构保护**。关闭 Bearer Auth，恢复官方原生 HttpOnly Cookie + `/auth/jwt/refresh` 自动轮转续期，根治 1 小时闲置 401 死锁。 |
| `ENABLE_NEW_APP_VIEWS` | `FEATURES.enableNewAppViews` | ❌ **关 (false)** | **经典收件箱/邮件列表架构**。已恢复为经典的 SoupView 视图布局，收件箱使用经典的单列宽幅邮件列表与全局筛选，取消分栏组合卡片模式。 |
| `ENABLE_SIDEBAR_NEXT` | `enable-new-app-views` | ❌ **关 (false)** | **经典展开式侧边栏**。已恢复为官方经典侧边栏（AppSidebar），提供完整的图标与文字导航。 |
| `ENABLE_COGNITION` | `FEATURES.cognition` | ✅ **开 (true)** | **微服务依赖**。挂载后端 `document_cognition_service` 容器，支持文档全文解析与智能索引。 |
| `ENABLE_AGENTS` | `FEATURES.agents` | ✅ **开 (true)** | **微服务依赖**。开启后端智能体执行与 Toolset 调度核心通道。 |
| `ENABLE_CRM` | `FEATURES.enableCrm` | ✅ **开 (true)** | **微服务依赖**。开启客户关系与联系人数据存储，对接 `contacts_service`。 |
| `ENABLE_SCHEDULED_ACTIONS` | `FEATURES.scheduledActions` | ✅ **开 (true)** | **自动化功能**。已在生产开启；前端自动化面板与定时任务配置器已全面解锁，接口返回空列表（Caddy 保底）。 |
| `ENABLE_DOCS_COLLAB` | `FEATURES.docsCollab` | ❌ **关 (false)** | **微服务依赖**。协同画布 Token 鉴权服务未在轻量单机编排，使用通用 Markdown 协同模式。 |
| `ENABLE_SEARCH_SERVICE` | - | ✅ **开 (true)** | **微服务依赖**。对接 OpenSearch 集群与 `search_processing_service`。 |
| `ENABLE_EMAIL` | - | ✅ **开 (true)** | **微服务依赖**。对接 `email_service` 与 `email_pubsub_workers`。 |
| `ENABLE_DIRECT_ATTACHMENT_DOWNLOAD` | - | ✅ **开 (true)** | **数据流向**。邮件大附件绕过 DSS 服务直连存储下载，减轻应用节点负载。 |
| `ENABLE_PROXY_EMAIL_IMAGES` | - | ✅ **开 (true)** | **安全与隐私**。邮件内嵌图片通过后端 `image_proxy_service` 同源代理，隐藏客户端真实 IP。 |

### 2. 🏛️ 客户端数据与缓存通道（共 7 项）

| 统一配置项 | 内部键名 | 生产状态 | 运行考量 |
| :--- | :--- | :---: | :--- |
| `ENABLE_GRAPHQL_SOUP` | `enable-graphql-soup` | ❌ **关 (默认)** | 保持成熟稳定的 REST/DSS 通道，不使用实验性 GraphQL Soup。 |
| `ENABLE_GRAPHQL_BACKFILL` | - | ✅ **开 (true)** | 开启前端 GraphQL 数据预热与缓存回填加速。 |
| `DISABLE_BROWSER_TURSO_CACHE`| - | ❌ **关 (默认)** | 不禁用浏览器本地 Turso SQLite 缓存，保障秒级交互体验。 |
| `ENABLE_SOUP_FILTER_PERSISTENCE`| `enable-soup-filter-persistence` | ✅ **开 (true)** | 列表筛选条件持久化保存在客户端 localStorage。 |
| `ENABLE_INBOX_NOTIFIED_SORT` | - | ❌ **关 (默认)** | 保持关闭，避免邮件列表按通知状态频繁重排。 |
| `ENABLE_CLIENT_EMAIL_SIGNAL_FILTER` | - | ❌ **关 (默认)** | 保持关闭，统一由后端进行信号过滤。 |
| `ENABLE_INBOX_RESYNC` | - | ✅ **开 (true)** | **强制同步入口（能力）**。已开启；解锁邮箱设置中的强制同步按钮（Force sync），允许用户在需要时手动触发即时增量拉取。 |
| `ENABLE_INBOX_SYNC_STATUS` | - | ✅ **开 (true)** | **同步状态指示（状态）**。已开启；展示邮箱实时同步状态徽章、Gmail 历史拉取百分比进度条（BackfillProgressBar）及全局同步成功/失败 Toast 提示。 |

---

### 3. ✨ 功能级业务开关（共 33 项，已剔除架构重复项）

| 功能配置项 | 关联 PostHog Key | 生产状态 | 业务领域与核心能力 |
| :--- | :--- | :---: | :--- |
| `ENABLE_CHAT_V3_AGENTS` | - | ✅ **开 (true)** | **AI**：频道内 `@mention` 呼出智能体交互。 |
| `ENABLE_AGENT_SESSION_COMPOSER` | - | ✅ **开 (true)** | **AI**：新建会话时的模型挑选器与自定义 Prompt 面板。 |
| `ENABLE_CURSOR_AGENTS` | - | ✅ **开 (true)** | **AI**：集成 Cursor Agents 开发者助理。 |
| `INLINE_AI_EDITING` | - | ✅ **开 (true)** | **AI**：文本划词浮动 AI 润色、翻译、扩写胶囊工具。 |
| `ENABLE_MARKDOWN_AI_GENERATE` | - | ✅ **开 (true)** | **AI**：AI 快速生成结构化 Markdown 大纲与全文。 |
| `ENABLE_HOME_RECOMMENDATIONS` | - | ✅ **开 (true)** | **AI**：工作台首页基于进度的智能待办建议。 |
| `ENABLE_EMAIL_SIGNATURES` | - | ✅ **开 (true)** | **邮件**：富文本个性化签名与发信自动挂载。 |
| `ENABLE_EMAIL_SCHEDULED_SEND` | - | ✅ **开 (true)** | **邮件**：邮件定时排期发送（Send Later）。 |
| `ENABLE_MULTI_INBOX` | - | ✅ **开 (true)** | **邮件**：多邮箱统一聚合与无缝切换收发。 |
| `ENABLE_SNIPPETS` | - | ✅ **开 (true)** | **邮件**：快捷回复文本片段（`;` 键呼出）。 |
| `ENABLE_CHAT_CHANNEL_ATTACHMENT` | - | ✅ **开 (true)** | **协作**：团队频道拖拽发送附件文件与图片。 |
| `BOT_MANAGEMENT` | - | ✅ **开 (true)** | **协作**：团队机器人与自动化交互配置面板。 |
| `ENABLE_TAG_TEAM_SHARING` | - | ✅ **开 (true)** | **协作**：个人标签与团队共享标签互通。 |
| `ENABLE_PROJECT_SHARING` | - | ✅ **开 (true)** | **协作**：项目与文件夹对外公开只读链接分享。 |
| `ENABLE_NOTIFICATION_SETTINGS` | - | ✅ **开 (true)** | **协作**：精细化通知偏好与推送通道控制。 |
| `ENABLE_ACTIVITY_FEED` | - | ✅ **开 (true)** | **协作**：全局成员与团队动态流水。 |
| `ENABLE_RECENT_VIEW` | - | ✅ **开 (true)** | **视图**：最近访问与编辑的历史记录视图。 |
| `ENABLE_ENTITY_ACTIVITY_SECTION` | - | ✅ **开 (true)** | **视图**：实体详情侧边栏中的互动时间线。 |
| `ENABLE_HOME_VIEW` | `enable-home-view` | ✅ **开 (true)** | **视图**：个人专属工作台 Dashboard 总览。 |
| `ENABLE_CALENDAR_UI` | - | ✅ **开 (true)** | **日历**：日历主界面与排期视图。 |
| `ENABLE_CALENDAR_SEARCH_UI` | - | ✅ **开 (true)** | **日历**：日历事件全文搜索面板。 |
| `ENABLE_CALENDAR_TEAM_OOO` | - | ✅ **开 (true)** | **日历**：团队成员休假/外出（OOO）排期可视化。 |
| `ENABLE_CALENDAR_PROMPT_WEB` | `enable-calendar-prompt-web` | ✅ **开 (true)** | **日历**：Web 端快捷绑定与同步日历提醒气泡。 |
| `ENABLE_REMINDERS` | - | ✅ **开 (true)** | **待办**：邮件与待办定时提醒（快捷键 `h`）。 |
| `ENABLE_SOUP_GROUP_BY` | `enable-soup-group-by` | ✅ **开 (true)** | **列表**：高级聚合分组（按状态、客户、优先级）。 |
| `ENABLE_TASK_DUPLICATES` | `enable-task-duplicates` | ✅ **开 (true)** | **列表**：相似任务与重复任务智能检测。 |
| `ENABLE_UNIFIED_LIST_AI_INPUT` | - | ✅ **开 (true)** | **列表**：顶部 AI 自然语言快速创建任务。 |
| `ENABLE_MARKDOWN_COMMENTS` | - | ✅ **开 (true)** | **文档**：行级划词批注与协同评论。 |
| `ENABLE_MARKDOWN_DIFF` | - | ✅ **开 (true)** | **文档**：历史修改可视化差异对比（Visual Diff）。 |
| `ENABLE_MARKDOWN_SIDE_PANEL` | - | ✅ **开 (true)** | **文档**：右侧大纲目录与属性侧边栏。 |
| `ENABLE_MARKDOWN_SEARCH_TEXT` | - | ✅ **开 (true)** | **文档**：正文内容毫秒级全文精确检索。 |
| `ENABLE_MARKDOWN_LIVE_COLLABORATION`| - | ✅ **开 (true)** | **文档**：Markdown 编辑时光标协同位置同步。 |
| `ENABLE_DOCUMENT_MENTION_NOTIFICATIONS`| - | ✅ **开 (true)** | **文档**：正文中 `@成员` 时自动触发系统提醒。 |
| `ENABLE_HISTORY_COMPONENT` | - | ✅ **开 (true)** | **文档**：详细历史版本时间轴回溯。 |
| `ENABLE_GIT_BLAME` | - | ✅ **开 (true)** | **文档**：行级作者追溯视图。 |
| `ENABLE_REFERENCES_MODAL` | - | ✅ **开 (true)** | **文档**：双向链接与反向引用快捷弹窗。 |
| `ENABLE_MENTION_TRACKING` | - | ✅ **开 (true)** | **文档**：全局提及跟踪与关联汇总。 |
| `ENABLE_PR_DISCUSSION_INPUT` | - | ✅ **开 (true)** | **开发**：GitHub PR 讨论面板与 Code Review 直接回复。 |
| `ENABLE_ONBOARDING_V4` | - | ✅ **开 (true)** | **引导**：全新 V4 全屏开箱即用向导。 |
| `ENABLE_NEW_PRICING` | - | ✅ **开 (true)** | **商业**：高级定价与会员升级收银台界面展示。 |
| `ENABLE_STATIC_DOCUMENT_CARDS` | - | ❌ **关 (默认)** | **渲染**：保持动态卡片渲染，不降级为静态模式。 |
| `ENABLE_SCRIPTING` | - | ❌ **关 (默认)** | **安全**：关闭 PDF 内置脚本解析，防止跨站安全隐患。 |

---

## 四、真实 RBAC 权限与角色（后端鉴权真相）

### 1. 核心权限机制
前端实际通过 `useHasPermission(PERMISSION_IDS.XXX)` 进行权限校验，数据来源于数据库 `roles_and_permissions`。

| 后端权限 ID (`PermissionId`) | 前端权限常量 (`PERMISSION_IDS`) | 对应权益作用 |
| :--- | :--- | :--- |
| `read:professional_features` | `READ_PROFESSIONAL_FEATURES` | **专业版核心权限**：解锁付费高级功能，去除功能封锁。 |
| `write:proai` | `WRITE_PROAI` | **顶级 AI 模型权限**：允许调用 Claude Opus、GPT-5.6 等旗舰模型。 |
| `write:ai_features` | `WRITE_AI_FEATURES` | **AI 基础功能权限**：允许使用智能体与润色工具。 |
| `write:admin_panel` | `WRITE_ADMIN_PANEL` | **系统管理特权**：访问后台运维面板与健康状态。 |
| `write:it_panel` | `WRITE_IT_PANEL` | **组织 IT 权限**：管理企业成员与权限分配。 |
| `write:email_tool` | `WRITE_EMAIL_TOOL` | **邮件高级工具权限**。 |
| `read:docx_editor` | `READ_DOCX_EDITOR` | **Docx 高级编辑器权限**。 |

### 2. 数据库核心角色 (`RoleId`)
- `super_admin`：系统超级管理员，包含所有权限。
- `corporate`：企业协议版，去除邮件底部水印。
- `professional_subscriber` / `team_subscriber`：付费专业版/团队版。
- `sub_opus`：Opus AI 订阅角色。
- `ai_subscriber`：AI 订阅角色。

> **提示**：生产宿主机的 `ADMIN_EMAIL=hello@chnprints.com` 在数据库迁移与初始化时已绑定了全部 15 个数据库角色，因此登录该账号即拥有完整特权。
